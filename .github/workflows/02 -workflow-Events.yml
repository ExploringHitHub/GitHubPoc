name: 02 -workflow Events
 
on: pull_request
 
 jobs:
    echo1:
        runs-on: ubuntu-lastest
        steps:
            -name: Show the trigger
            run:  echo" this trigger is generated by :${{github.event.name}}"

