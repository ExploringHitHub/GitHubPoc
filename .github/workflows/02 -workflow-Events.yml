name: 02 -workflow Events
on:
  workflow_dispatch:
jobs:
  echo1:
    runs-on: ubuntu-latest
    steps:
      - name: Show Trigger Name
        run: |
          echo "this trigger is generated by : ${{ github.event_name }} event."
          echo "exit with zero code"
          exit 0

