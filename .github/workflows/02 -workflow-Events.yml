name: 02 -workflow Events
 
on: pull_request
jobs:
    echo1:
        runs-on: ubuntu-latest
        steps:
          - name: Show Trigger Name
            run:  echo" this trigger is generated by :${{github.event_name}}"

