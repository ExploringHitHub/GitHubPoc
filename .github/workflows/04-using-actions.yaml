name: 04 - Using Actions
on: 
    push:
jobs:
    build:
     runs-on: ubuntu-latest
     steps:
        - name: Checkout Code
          uses: actions/checkout@v2

        - name: Printing Folders
          run: |
            echo "printing the folder structure after the checkout"
            ls


       
  