name: my-first-workflow
on: push
jobs:
  my-first-job:
  runs-on: ubuntu-latest
  steps:
    -name: echo hello world
     run: $echo" hello world"
  my-second-job:
  runs-on: ubuntu-latest
  steps:
    -name: echo bye bye world
     run:$echo"bye bye world"
